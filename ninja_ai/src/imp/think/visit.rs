use crate::imp::structs::ai::cost_map::CostMap;
use crate::imp::structs::event::Event;
use crate::imp::structs::events::Events;

pub(crate) fn visit(ev : &Event, evs : &Events, cost_map : &mut CostMap){
    for w in ev.walls(){

    }
}